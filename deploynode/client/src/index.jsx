/* This file loads all the UI components and  action for th eUI components*/

import React from 'react/addons';

React.initializeTouchEvents(true);

document.addEventListener('DOMContentLoaded', (event) => {

});